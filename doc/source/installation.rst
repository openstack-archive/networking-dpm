============
Installation
============

At the command line::

    $ pip install networking-dpm

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-dpm
    $ pip install networking-dpm
