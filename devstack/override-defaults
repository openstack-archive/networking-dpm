# Configure Mechanism Driver
# Gets overwritten in neutron-legacy if "Q_DVR_MODE" != "legacy"
Q_ML2_PLUGIN_MECHANISM_DRIVERS=${Q_ML2_PLUGIN_MECHANISM_DRIVERS:-openvswitch,dpm}

# Configure provider networking
Q_USE_PROVIDER_NETWORKING=True
PROVIDER_SUBNET_NAME="provider_net"
PROVIDER_NETWORK_TYPE="flat"
# andreas_s (Feb 2017): Required for provider networking
USE_SUBNETPOOL=False